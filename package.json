{
  "name": "storyscroller",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "StoryScroller React component and demo extracted from portfolio monorepo",
  "author": "Prime Inc",
  "license": "MIT",
  "engines": {
    "node": ">=20.17.0",
    "pnpm": ">=9.12.0"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "turbo run dev --filter demo",
    "dev:package": "turbo run dev --filter package",
    "build": "turbo run build",
    "build:package": "turbo run build --filter package",
    "build:demo": "turbo run build --filter demo",
    "test": "turbo run test",
    "test:package": "turbo run test --filter package",
    "test:demo": "turbo run test --filter demo",
    "test:e2e": "turbo run test:e2e --filter demo",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "install:all": "pnpm install"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "rimraf": "^6.0.1",
    "turbo": "^2.0.0"
  }
}